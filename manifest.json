{
  "schemaVersion": 1,
  "id": "kaufmann.omarag",
  "name": "OMA",
  "version": "0.1.0",
  "author": "Stephan Kaufmann",
  "license": "MIT",
  "description": "Local knowledge. Ask your own documents and see where every answer comes from.",
  "kinds": [
    "service",
    "overlay",
    "bar-widget"
  ],
  "entryPoints": {
    "overlay": "OMA.qml",
    "barWidget": "BarWidget.qml",
    "service": "service/OmaService.qml"
  },
  "settings": {
    "defaults": {
      "backendWhenClosed": "Stop with OMA",
      "answerModel": "",
      "omaFontScale": 1.0,
      "omaFontFamily": "",
      "notifySound": "/usr/share/sounds/freedesktop/stereo/complete.oga",
      "compactMode": false,
      "windowOpacity": 1.0
    },
    "schema": [
      {
        "key": "backendWhenClosed",
        "type": "enum",
        "label": "Backend when OMA is closed",
        "description": "Stop with OMA frees everything the moment you close it — the server and the models it loaded — and the next question waits for both to come back. Keep running holds the server, roughly 430 MB, and leaves loaded models in place until they time out, so the next question starts straight away.",
        "options": [
          "Stop with OMA",
          "Keep running"
        ],
        "defaultValue": "Stop with OMA"
      },
      {
        "key": "answerModel",
        "type": "string",
        "label": "Answering model",
        "description": "Passed to `lilbee ask --model`. Leave empty to use whatever lilbee is configured with. Useful when the configured model is too large to load next to the retrieval models.",
        "defaultValue": ""
      },
      {
        "key": "omaFontScale",
        "type": "number",
        "label": "Text size",
        "description": "A factor on the shell's text sizes, applied inside OMA only. 1.0 is exactly what the rest of Omarchy uses. The shell's own font is never changed.",
        "defaultValue": 1.0
      },
      {
        "key": "omaFontFamily",
        "type": "string",
        "label": "Font",
        "description": "Font family for OMA alone. Leave empty to follow the shell's font, which is what `omarchy font set` configures.",
        "defaultValue": ""
      },
      {
        "key": "notifySound",
        "type": "string",
        "label": "Sound when an answer arrives",
        "description": "Played only when the answer finishes while OMA is closed. Any file the desktop can play; leave empty for silence. Point it at the same file another tool uses and you get the same sound.",
        "defaultValue": "/usr/share/sounds/freedesktop/stereo/complete.oga"
      },
      {
        "key": "compactMode",
        "type": "bool",
        "label": "Compact window",
        "description": "Opens a small window under the bar icon showing the chat alone, instead of the large one in the middle of the screen. The icon in the header switches between them.",
        "defaultValue": false
      },
      {
        "key": "windowOpacity",
        "type": "number",
        "label": "Window opacity",
        "description": "How solid the window's background is, from 1.0 for opaque down to 0.45. Only the background fades — the text stays fully legible, because this is a window for reading numbers out of a textbook.",
        "defaultValue": 1.0
      }
    ]
  },
  "barWidget": {
    "displayName": "OMA",
    "description": "Open your local knowledge base.",
    "category": "AI",
    "allowMultiple": false
  }
}
